/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/bootstrap/cache/config.php
/vendor
.env
.env.backup
.env.production
.env.testing
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/.vapor
/package-lock.json
.env.uk_live

# Sentry Config File
.env.sentry-build-plugin

.octogent

# Build product written by `npm run release:stamp` immediately before a deploy.
# It carries the commit into the Vapor artifact so /health can say what is
# actually live. Never commit it — a committed stamp is stale on arrival.
.release
