# Daily report — Thursday 21 August 2026

Format as specified in the Developer Master Plan, 19 Aug 2026, §F.

---

## Done today

⚠️ **Everything below is LOCAL. Nothing is on staging and nothing is live.**
There is no sandbox app to deploy to and no tagged release has ever been made —
see *Blocked* below. Every item is written, reviewed and passing its tests on a
developer machine only.

| Row | What | Where |
|---|---|---|
| **A3** | Sections 3 and 6 flipped from COMING SOON to **LIVE NOW** (`bio_direct_sales`). They shipped against the brief's own label because the bio page could not sell; the B stream has since landed, so the label is now true. Verified before flipping: `bio.buy` routed, `creator_bio_items` migrated, the four editor endpoints present, 19 tests passing. | local |
| **B** | Bio direct sales verified **end to end in a browser** for the first time — card → `/bio/buy/{uuid}` → the item's real checkout. Previously only the test suite had been run. | local |
| **B** | Bio editor rebuilt to look like the page it edits: the item list now renders exactly as a supporter sees it, social accounts split from Spenny Piggy pages, one-tap item adding with thumbnails, live destination preview as a username is typed. | local |
| **B** | Bug: the listing brand-name rule was applied to social link buttons, so **5 of the 7 platforms refused their own name** ("TikTok", "Instagram", "YouTube", "Twitch", "Spotify"). Fixed with a narrow per-platform allowance; other brands still refused. | local |
| **E** | **Admin Discovery controls** — the performance and attribution half. New `/discovery` screen: SP-generated vs creator-generated traffic, earnings from the ledger, a source-by-source breakdown, which creators Discovery is working for, and unattributed rows reported rather than hidden. Read-only, enforced by test. | local |
| **E** | **Enhanced Audit Logs — the Stripe half.** Refunds now write an audit trail (attempt → succeeded/failed). Before today, changing the *date* on a task purchase wrote an audit row and **refunding real money wrote nothing**. | local |
| **E** | **Discovery Phase 5 — the ten collections**, built as reusable components with one definition each, attribution keys for all ten, and the eligibility rule extracted so it is no longer duplicated across three services. | local |
| **E** | **Discovery Phase 6 — first surface**: payment success "Discover someone else". The `payment-success` attribution key has been reserved since Phase 1 and tagged nothing, because the prompt did not exist. | local |
| — | Header nav: Oink Store replaced with **Link in Bio** (the store is kill-switched; the nav's most valuable slot pointed at something nobody can buy from). Oink Store moved to the footer with a **Coming soon** badge. | local |
| — | Sitemap: `/creators/discovery` and `/creators/link-in-bio` were **missing** while the other six creator pages were listed — the two pages about to carry paid ads. | local |

**Tests:** spennypiggy.co and admin.spennypiggy.co both green, 0 failures.
Roughly 30 tests added today across the items above.

---

## Released?

**No.**

- Release tag: **none** — `git tag` is empty in both repositories and always has been.
- Regression result: **not run** — the 75-row checklist exists; there is no sandbox to run it against.
- Rollback point: **none taken.**

This is the same answer as every previous day, and it is the item blocking A, B
and Phase 4 from going live.

---

## In progress (tomorrow)

- Discovery Phase 6 — remaining surfaces: homepage collections, Discover, content pages, empty states, emails. *(Plan target: Sept/Oct — running ahead.)*
- Admin Discovery controls — the remaining five capabilities: enable/disable collections, feature/remove creators, eligibility, preview/re-run selections, history. *(Sept/Oct)*
- Supporter re-engagement and push. *(Sept/Oct)*
- Infrastructure Scaling Plan. *(No date set.)*

---

## Blocked / needs Jack

1. **Section F, all of it.** No sandbox app exists (own database, own Stripe test keys, own domain); nobody has confirmed the current `development` environment uses Stripe **test** keys; no release has ever been tagged; rollback has never been tested. Until this exists, "regression → live" cannot happen for A, B or Phase 4, whatever state the code is in.
2. **A3 section 6 copy.** The section is now labelled LIVE NOW and its copy reads *"Choose which items appear, in what order, and what it looks like."* The first two are live; **there is no theme, colour or appearance control** in the bio editor. Either those three words come out of the copy or a control needs building. Copy is client-final, so this is your call.
3. **🚨 Eight identity documents are publicly readable.** Verified 8 of 8 on the development database: real ID cards and a residence permit returning HTTP 200 from the CDN with no authentication and no expiry. **The same check needs running against production and the files deleting.** Not done here deliberately — it is irreversible, it is live personal data, and some may be dispute evidence.
4. **Uploadcare "Secure delivery"** needs enabling in the dashboard and its key adding, or paid content URLs stay permanent and unrevocable.
5. **Roadmap priorities 1, 2, 4, 5, 8, 9** — waiting on your order.
6. **Link in Bio has not been tested in a real in-app browser.** The page's own LIVE NOW claim is "built for the phone", and the whole premise is an Instagram bio link — which opens in Instagram's own webview, not Safari. Only a desktop browser narrowed to phone width has been used. Ten minutes on a real phone would settle it.

---

## Security ticks

- **Enhanced Audit Logs (§E) — Stripe half closed.** Every refund now writes an audit trail, including the attempt *before* the Stripe call: if the call times out after Stripe has created the refund, logging only outcomes would record a failure for money that actually moved.
- Refund audit entries are scrubbed through the same redactor the security alerts use — a raw Stripe error message names account and payment ids, and an audit trail is read by more people than the logs are.
- No Section D rows were open to close today; §D §3 was delivered previously.
