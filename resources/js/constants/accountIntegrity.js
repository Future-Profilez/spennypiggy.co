/**
 * The one sentence the platform says about honest details.
 *
 * 🚨 EVERYTHING A CREATOR DOES PUBLISHES ITSELF NOW (11 Sep 2026, client direction), so
 * the only thing standing between a fake profile and a live page is that the creator was
 * told, plainly and in the same words everywhere, what happens if the details are not
 * real. Review still happens — it is OUR internal work and is never drawn as a queue on a
 * creator's screen — and this is the one place that consequence is written down.
 *
 * ⚠️ ONE STRING, imported wherever details are entered. Three surfaces each writing their
 * own version is how a warning ends up strict on one screen and soft on the next, which is
 * the inconsistency this whole pass exists to remove.
 *
 * ⚠️ It promises no wait and names no team. It states a consequence, which is true, rather
 * than a process, which is not.
 */
export const REAL_DETAILS_WARNING =
    "Use your real details. Accounts built on fake, borrowed or copied details are removed, and any balance on them is not paid out.";

/** The short form, for a field hint where the full sentence would crowd the input. */
export const REAL_DETAILS_SHORT = "Real details only — fake accounts are removed.";
